services:
  frontend:
    build:
      context: .
    develop:
      watch:
        - action: sync
          path: ./src
          target: /deciphraze/src
        - action: rebuild
          path: ./package.json
    ports:
      - "1111:1111"